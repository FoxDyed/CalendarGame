# Main Gameplay Scene Tree (Shell)

- MainGameplayScene (`Control`)
  - RootMargin (`MarginContainer`)
    - MainLayout (`VBoxContainer`)
      - UIControls (`PanelContainer`)
        - UIRow (`HBoxContainer`)
          - SceneTitle (`Label`)
          - StatusLabel (`Label`)
          - ActionButton (`Button`)
      - PlayArea (`HSplitContainer`)
        - Board (`PanelContainer`)
          - BoardCenter (`CenterContainer`)
            - BoardLabel (`Label`)
        - PieceTray (`PanelContainer`)
          - TrayCenter (`CenterContainer`)
            - TrayLabel (`Label`)
